<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covaxin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covaxin">
        <activity android:name=".DownloadCertifacte">

        </activity>
        <activity android:name=".SearchByDistrict">
        </activity>
        <activity android:name=".SearchByPin" />
        <activity android:name=".ShowDetailsUser" />
        <activity android:name=".IDProofDetails" >

        </activity>
        <activity android:name=".Confirmotp" />
        <activity android:name=".SendOTP" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>